//declaro una variable tipo number
let n:number ;
n = 5;

// esto dará un error de compilación (ver error del entorno de trabajo)
// n = "hola";

// declaro una función que toma dos números, y retorna un número
function sumar(a:number,b:number):number{
    return a-b 
}
